{
  "name": "@0xmux/${PLATFORM}",
  "version": "${VERSION}",
  "description": "0xMux binary for ${OS} ${CPU}",
  "license": "MIT",
  "os": ["${OS}"],
  "cpu": ["${CPU}"],
  "bin": {
    "oxmux-server": "bin/oxmux-server"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/DLHTX/0xMux"
  }
}
